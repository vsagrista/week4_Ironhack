<h1>Current Product: <%= @product.title %></h1>
<p> Minimunt bet for this product: $<%= @product.min_bid %></p>
<%= form_for [@product, @bid] do |f| %>
	

	<%= f.label :amount %>
	<%= f.number_field :amount %>
	<%= f.label :email %>
	<%= f.text_field :user %>
	<%= f.submit "Bid for Product" %>
<% end %>


<!-- Here goes the bid 
	IT has a form for products that goes to create. 
	They put their email address and the amount
